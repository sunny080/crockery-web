import * as React from 'react'

import ParallexSection from './ParallexSection'

export default {
  title: 'Sections/ParallexSection',
  component: ParallexSection,
}

const Template = (args) => <ParallexSection {...args} />

export const Default = Template.bind({})
Default.args = {}
